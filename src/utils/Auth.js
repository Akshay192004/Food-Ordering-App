
export const login = (username, password) => {
    // Example: You can check hardcoded credentials or make a request to your server
    if (username === 'admin' && password === 'password') {
      return true; 
    } else {
      return false; 
    }
  };
  